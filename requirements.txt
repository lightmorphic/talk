# Whisper via CTranslate2. Replaced onnx-asr/Parakeet, which
# dropped whole sentences out of long dictations.
faster-whisper>=1.2.0
# Pinned below 2 deliberately. faster-whisper only floors this, and 1.0
# was a breaking release (requests to httpx, several arguments removed).
# Nothing here calls what went, but an unpinned rebuild is free to pick
# up the next such release without anyone deciding to.
huggingface-hub>=0.30,<2
sounddevice>=0.5.0
# X11 keystroke injection (XTEST). Unused on Wayland, where the portal
# does the typing, but the one binary has to cover both.
pynput>=1.8.0
# Imported directly (config.py, updater.py) to point the bundled Python
# at a real CA bundle. It arrives as someone else's dependency today,
# which means a version bump elsewhere could take it away from a build
# that imports it by name.
certifi
